services:
  aegis-assistant:
    build: .
    container_name: aegis-rag-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./vectorstore_index:/app/vectorstore_index
    restart: unless-stopped
